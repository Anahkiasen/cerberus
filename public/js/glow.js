// Generated by CoffeeScript 1.4.0
(function() {

  window.Glow = {
    /*
      Base URL to project.
      @type String
    */

    base: "%BASE%",
    /*
      The URL library is responsible for
      generating URLs to framework routes.
    */

    URL: {
      /*
          Retrieve the base URL.
          @return String
      */

      base: function() {
        return Glow.base;
      },
      /*
          Retrieve the URL to a route.
          @param  String  uri
          @param  Boolean   https
          @return String
      */

      to: function(uri, https) {
        var url;
        if (https == null) {
          https = false;
        }
        url = Glow.base + "/" + uri;
        if (https) {
          url.replace("http://", "https://");
        }
        return url;
      },
      /*
          Retrieve the secure URL to a route.
          @param  String uri
          @return String
      */

      to_secure: function(uri) {
        return Glow.URL.to(uri, true);
      }
    }
  };

}).call(this);
